// 1. variable
let myName = 'Khan';

// math operation: +, -, *, /
// shorthand: +=, --, *=, /=
// ++, --

// 2. Array
let friends = ['abul', 'babul', 'kabul', 'dabul', 'ebul'];

let thirdFriend = friends[2];
friends[3] = 'dambool';

// 3. conditionals
// >, <, >=, <=, ==, ===, !=, !==

if (friends.length < 2) {
    console.log('fokir tor kono friend nei');
}
else {

}

// 4. loop
let number = 0;
while (number < 6) {
    console.log(number);
    number++;
}

for (let i = 0; i <= 10; i++) {

}

// 5. function
function isMoonUp () {
    if (time > 7) 
        return true;
}

// 6. Object

let description = {
    height: 165, 
    manner: 'good',
}