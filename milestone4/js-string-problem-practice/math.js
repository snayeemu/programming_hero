const lightSpeed = 3 * Math.pow(10, 8);
console.log(lightSpeed);

const age1 = 25;
const age2 = 45;

const gap = Math.abs(age1 - age2);

if (gap < 5)
    console.log('you guys can be friends');
else 
    console.log('you guys stay apart');

const number = 2.5;
const fullNumber = Math.round(number);
console.log(fullNumber);

const ceil = Math.ceil(number);
console.log(ceil);

const floor = Math.floor(number);
console.log(floor);

console.log(Math.round( Math.random() * 10 + 90));